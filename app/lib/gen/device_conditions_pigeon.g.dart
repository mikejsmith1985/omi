// Autogenerated from Pigeon (v26.1.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: public_member_api_docs, non_constant_identifier_names, avoid_as, unused_import, unnecessary_parenthesis, prefer_null_aware_operators, omit_local_variable_types, unused_shown_name, unnecessary_import, no_leading_underscores_for_local_identifiers

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List, Uint8List;

import 'package:flutter/foundation.dart' show ReadBuffer, WriteBuffer;
import 'package:flutter/services.dart';

PlatformException _createConnectionError(String channelName) {
  return PlatformException(
    code: 'channel-error',
    message: 'Unable to establish connection on channel: "$channelName".',
  );
}
bool _deepEquals(Object? a, Object? b) {
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed
        .every(((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]));
  }
  if (a is Map && b is Map) {
    return a.length == b.length && a.entries.every((MapEntry<Object?, Object?> entry) =>
        (b as Map<Object?, Object?>).containsKey(entry.key) &&
        _deepEquals(entry.value, b[entry.key]));
  }
  return a == b;
}


/// How hot the device is, as the operating system sees it.
///
/// Both platforms report a graded state rather than a temperature, which is what we
/// want: the grades already account for the hardware, and a raw figure would need a
/// per-device threshold table that would be wrong on the next phone.
enum ThermalState {
  /// Nothing to worry about.
  nominal,
  /// Warm. Sustained heavy work is starting to cost.
  fair,
  /// Hot. The system is throttling, and a long import will make it worse.
  serious,
  /// Very hot. The system is taking measures of its own.
  critical,
  /// The platform did not say.
  unknown,
}

/// The phone's power and thermal state at one moment.
class DeviceConditions {
  DeviceConditions({
    required this.batteryLevel,
    required this.isCharging,
    required this.isPowerSaveMode,
    required this.thermalState,
  });

  /// Battery charge from 0 to 1, or -1 when the platform declined to say.
  double batteryLevel;

  /// Whether the phone is plugged in.
  ///
  /// The single most important field here: a queue that would be rude to run on
  /// battery is entirely reasonable to run while charging, which is exactly when
  /// someone would leave a backlog importing overnight.
  bool isCharging;

  /// Whether the user has asked the system to conserve power.
  ///
  /// Distinct from a low battery: it is an explicit instruction, and running a long
  /// background job through it is ignoring what the user asked for.
  bool isPowerSaveMode;

  /// How hot the device is.
  ThermalState thermalState;

  List<Object?> _toList() {
    return <Object?>[
      batteryLevel,
      isCharging,
      isPowerSaveMode,
      thermalState,
    ];
  }

  Object encode() {
    return _toList();  }

  static DeviceConditions decode(Object result) {
    result as List<Object?>;
    return DeviceConditions(
      batteryLevel: result[0]! as double,
      isCharging: result[1]! as bool,
      isPowerSaveMode: result[2]! as bool,
      thermalState: result[3]! as ThermalState,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! DeviceConditions || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(encode(), other.encode());
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => Object.hashAll(_toList())
;
}


class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();
  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    }    else if (value is ThermalState) {
      buffer.putUint8(129);
      writeValue(buffer, value.index);
    }    else if (value is DeviceConditions) {
      buffer.putUint8(130);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129: 
        final int? value = readValue(buffer) as int?;
        return value == null ? null : ThermalState.values[value];
      case 130: 
        return DeviceConditions.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

/// Dart -> native.
class DeviceConditionsHostApi {
  /// Constructor for [DeviceConditionsHostApi].  The [binaryMessenger] named argument is
  /// available for dependency injection.  If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  DeviceConditionsHostApi({BinaryMessenger? binaryMessenger, String messageChannelSuffix = ''})
      : pigeonVar_binaryMessenger = binaryMessenger,
        pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty ? '.$messageChannelSuffix' : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  /// Reads the phone's current power and thermal state.
  ///
  /// Polled rather than subscribed: the queue only needs this between recordings, and
  /// a subscription would keep a receiver alive for the entire life of the app to
  /// serve something consulted once every few minutes.
  Future<DeviceConditions> read() async {
    final String pigeonVar_channelName = 'dev.flutter.pigeon.omi_device_conditions.DeviceConditionsHostApi.read$pigeonVar_messageChannelSuffix';
    final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final List<Object?>? pigeonVar_replyList =
        await pigeonVar_sendFuture as List<Object?>?;
    if (pigeonVar_replyList == null) {
      throw _createConnectionError(pigeonVar_channelName);
    } else if (pigeonVar_replyList.length > 1) {
      throw PlatformException(
        code: pigeonVar_replyList[0]! as String,
        message: pigeonVar_replyList[1] as String?,
        details: pigeonVar_replyList[2],
      );
    } else if (pigeonVar_replyList[0] == null) {
      throw PlatformException(
        code: 'null-error',
        message: 'Host platform returned null value for non-null return value.',
      );
    } else {
      return (pigeonVar_replyList[0] as DeviceConditions?)!;
    }
  }
}
