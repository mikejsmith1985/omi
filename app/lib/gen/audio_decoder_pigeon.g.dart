// Autogenerated from Pigeon (v26.1.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: public_member_api_docs, non_constant_identifier_names, avoid_as, unused_import, unnecessary_parenthesis, prefer_null_aware_operators, omit_local_variable_types, unused_shown_name, unnecessary_import, no_leading_underscores_for_local_identifiers

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List, Uint8List;

import 'package:flutter/foundation.dart' show ReadBuffer, WriteBuffer;
import 'package:flutter/services.dart';

PlatformException _createConnectionError(String channelName) {
  return PlatformException(
    code: 'channel-error',
    message: 'Unable to establish connection on channel: "$channelName".',
  );
}

List<Object?> wrapResponse({Object? result, PlatformException? error, bool empty = false}) {
  if (empty) {
    return <Object?>[];
  }
  if (error == null) {
    return <Object?>[result];
  }
  return <Object?>[error.code, error.message, error.details];
}
bool _deepEquals(Object? a, Object? b) {
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed
        .every(((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]));
  }
  if (a is Map && b is Map) {
    return a.length == b.length && a.entries.every((MapEntry<Object?, Object?> entry) =>
        (b as Map<Object?, Object?>).containsKey(entry.key) &&
        _deepEquals(entry.value, b[entry.key]));
  }
  return a == b;
}


/// What a decode session is doing. Mirrored to Dart on every transition.
enum AudioDecodeState {
  idle,
  opening,
  decoding,
  finished,
  failed,
  cancelled,
}

/// What the decoder found in the file before decoding it.
///
/// Returned by probe() so an unsupported or empty recording is refused immediately,
/// rather than after the user has waited through a long decode (FR-003).
class AudioProbeResult {
  AudioProbeResult({
    required this.isDecodable,
    required this.durationSeconds,
    required this.sourceSampleRate,
    required this.sourceChannelCount,
    this.codecDescription,
    this.rejectionReason,
    this.creationEpochMillis,
  });

  /// Whether the platform decoder can read this file.
  bool isDecodable;

  /// How long the recording runs, in seconds. Drives the time estimate (FR-016).
  double durationSeconds;

  /// The source sample rate, before conversion.
  int sourceSampleRate;

  /// The source channel count, before downmixing.
  int sourceChannelCount;

  /// The codec the platform reported, for diagnostics and for naming a refusal.
  String? codecDescription;

  /// Why the file cannot be decoded, when [isDecodable] is false.
  String? rejectionReason;

  /// When the recording was made, from metadata inside the file, in epoch
  /// milliseconds — or null when the file carries no such date.
  ///
  /// This is the strongest evidence of a recording's true time and the only source
  /// that cannot be disturbed by copying the file around, so it is read here at probe
  /// time rather than guessed at later. Null is common and expected: plenty of
  /// recorders write no date at all, which is what the filename and the user are for.
  int? creationEpochMillis;

  List<Object?> _toList() {
    return <Object?>[
      isDecodable,
      durationSeconds,
      sourceSampleRate,
      sourceChannelCount,
      codecDescription,
      rejectionReason,
      creationEpochMillis,
    ];
  }

  Object encode() {
    return _toList();  }

  static AudioProbeResult decode(Object result) {
    result as List<Object?>;
    return AudioProbeResult(
      isDecodable: result[0]! as bool,
      durationSeconds: result[1]! as double,
      sourceSampleRate: result[2]! as int,
      sourceChannelCount: result[3]! as int,
      codecDescription: result[4] as String?,
      rejectionReason: result[5] as String?,
      creationEpochMillis: result[6] as int?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! AudioProbeResult || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(encode(), other.encode());
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => Object.hashAll(_toList())
;
}


class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();
  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    }    else if (value is AudioDecodeState) {
      buffer.putUint8(129);
      writeValue(buffer, value.index);
    }    else if (value is AudioProbeResult) {
      buffer.putUint8(130);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129: 
        final int? value = readValue(buffer) as int?;
        return value == null ? null : AudioDecodeState.values[value];
      case 130: 
        return AudioProbeResult.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

/// Dart -> native.
///
/// The decoder always produces **16 kHz, mono, signed 16-bit little-endian PCM**,
/// resampling and downmixing as needed. That shape is not a preference: the existing
/// test/unit/audio_source_test.dart pins PhoneMicSource at 320-byte frames described
/// as 10 ms at 16 kHz 16-bit mono, and frames that do not match will not survive the
/// pipeline.
///
/// Decoding is pull-based. Native decodes only when [readChunk] asks it to, so the
/// pump can bound how much audio is in flight (plan.md D-3). A push-based decoder
/// would race ahead of transcription and reintroduce the unbounded-memory failure
/// SC-005 exists to prevent.
class AudioDecoderHostApi {
  /// Constructor for [AudioDecoderHostApi].  The [binaryMessenger] named argument is
  /// available for dependency injection.  If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  AudioDecoderHostApi({BinaryMessenger? binaryMessenger, String messageChannelSuffix = ''})
      : pigeonVar_binaryMessenger = binaryMessenger,
        pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty ? '.$messageChannelSuffix' : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  /// Inspects a file without decoding it. Cheap, and safe to call before committing.
  ///
  /// Throws a PlatformException with code `file_unreadable` when the path cannot be
  /// opened at all.
  Future<AudioProbeResult> probe(String filePath) async {
    final String pigeonVar_channelName = 'dev.flutter.pigeon.omi_audio_decoder.AudioDecoderHostApi.probe$pigeonVar_messageChannelSuffix';
    final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[filePath]);
    final List<Object?>? pigeonVar_replyList =
        await pigeonVar_sendFuture as List<Object?>?;
    if (pigeonVar_replyList == null) {
      throw _createConnectionError(pigeonVar_channelName);
    } else if (pigeonVar_replyList.length > 1) {
      throw PlatformException(
        code: pigeonVar_replyList[0]! as String,
        message: pigeonVar_replyList[1] as String?,
        details: pigeonVar_replyList[2],
      );
    } else if (pigeonVar_replyList[0] == null) {
      throw PlatformException(
        code: 'null-error',
        message: 'Host platform returned null value for non-null return value.',
      );
    } else {
      return (pigeonVar_replyList[0] as AudioProbeResult?)!;
    }
  }

  /// Opens [filePath] for decoding under [sessionId] and resolves once ready.
  ///
  /// `sessionId` is minted by Dart and carried on every event, so a late event from an
  /// abandoned session cannot be mistaken for one from the current session — the same
  /// protection phone_mic_interface.dart uses, and for the same reason.
  ///
  /// Throws a PlatformException with one of: `file_unreadable`, `format_unsupported`,
  /// `no_audio_track`, `decoder_init_failed`.
  Future<void> openSession(String filePath, int sessionId) async {
    final String pigeonVar_channelName = 'dev.flutter.pigeon.omi_audio_decoder.AudioDecoderHostApi.openSession$pigeonVar_messageChannelSuffix';
    final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[filePath, sessionId]);
    final List<Object?>? pigeonVar_replyList =
        await pigeonVar_sendFuture as List<Object?>?;
    if (pigeonVar_replyList == null) {
      throw _createConnectionError(pigeonVar_channelName);
    } else if (pigeonVar_replyList.length > 1) {
      throw PlatformException(
        code: pigeonVar_replyList[0]! as String,
        message: pigeonVar_replyList[1] as String?,
        details: pigeonVar_replyList[2],
      );
    } else {
      return;
    }
  }

  /// Decodes and returns up to [maxBytes] of PCM, or an empty list at end of audio.
  ///
  /// The caller decides when to ask, which is what makes the pacing in plan.md D-3
  /// possible. Returns fewer bytes than asked for only at the end of the recording.
  Future<Uint8List> readChunk(int sessionId, int maxBytes) async {
    final String pigeonVar_channelName = 'dev.flutter.pigeon.omi_audio_decoder.AudioDecoderHostApi.readChunk$pigeonVar_messageChannelSuffix';
    final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[sessionId, maxBytes]);
    final List<Object?>? pigeonVar_replyList =
        await pigeonVar_sendFuture as List<Object?>?;
    if (pigeonVar_replyList == null) {
      throw _createConnectionError(pigeonVar_channelName);
    } else if (pigeonVar_replyList.length > 1) {
      throw PlatformException(
        code: pigeonVar_replyList[0]! as String,
        message: pigeonVar_replyList[1] as String?,
        details: pigeonVar_replyList[2],
      );
    } else if (pigeonVar_replyList[0] == null) {
      throw PlatformException(
        code: 'null-error',
        message: 'Host platform returned null value for non-null return value.',
      );
    } else {
      return (pigeonVar_replyList[0] as Uint8List?)!;
    }
  }

  /// Closes a session and releases the native decoder.
  ///
  /// Safe to call on a session that already finished or failed, because the pump's
  /// cleanup path must not need to know which happened.
  Future<void> closeSession(int sessionId) async {
    final String pigeonVar_channelName = 'dev.flutter.pigeon.omi_audio_decoder.AudioDecoderHostApi.closeSession$pigeonVar_messageChannelSuffix';
    final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[sessionId]);
    final List<Object?>? pigeonVar_replyList =
        await pigeonVar_sendFuture as List<Object?>?;
    if (pigeonVar_replyList == null) {
      throw _createConnectionError(pigeonVar_channelName);
    } else if (pigeonVar_replyList.length > 1) {
      throw PlatformException(
        code: pigeonVar_replyList[0]! as String,
        message: pigeonVar_replyList[1] as String?,
        details: pigeonVar_replyList[2],
      );
    } else {
      return;
    }
  }
}

/// Native -> Dart.
abstract class AudioDecoderFlutterApi {
  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  /// Reports a state transition for [sessionId].
  void onStateChanged(int sessionId, AudioDecodeState state);

  /// Reports that decoding failed, with a code matching the ones openSession throws.
  void onDecodeError(int sessionId, String code, String message);

  static void setUp(AudioDecoderFlutterApi? api, {BinaryMessenger? binaryMessenger, String messageChannelSuffix = '',}) {
    messageChannelSuffix = messageChannelSuffix.isNotEmpty ? '.$messageChannelSuffix' : '';
    {
      final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onStateChanged$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          assert(message != null,
          'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onStateChanged was null.');
          final List<Object?> args = (message as List<Object?>?)!;
          final int? arg_sessionId = (args[0] as int?);
          assert(arg_sessionId != null,
              'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onStateChanged was null, expected non-null int.');
          final AudioDecodeState? arg_state = (args[1] as AudioDecodeState?);
          assert(arg_state != null,
              'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onStateChanged was null, expected non-null AudioDecodeState.');
          try {
            api.onStateChanged(arg_sessionId!, arg_state!);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
    {
      final BasicMessageChannel<Object?> pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onDecodeError$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          assert(message != null,
          'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onDecodeError was null.');
          final List<Object?> args = (message as List<Object?>?)!;
          final int? arg_sessionId = (args[0] as int?);
          assert(arg_sessionId != null,
              'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onDecodeError was null, expected non-null int.');
          final String? arg_code = (args[1] as String?);
          assert(arg_code != null,
              'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onDecodeError was null, expected non-null String.');
          final String? arg_message = (args[2] as String?);
          assert(arg_message != null,
              'Argument for dev.flutter.pigeon.omi_audio_decoder.AudioDecoderFlutterApi.onDecodeError was null, expected non-null String.');
          try {
            api.onDecodeError(arg_sessionId!, arg_code!, arg_message!);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
  }
}
